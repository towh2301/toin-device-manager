import { registerRootComponent } from 'expo';

import App from './src/App'; // <-- Point this to your App.tsx

// It registers your App component as the root of the application.
registerRootComponent(App);
